{
  "name": "test-address",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --test 'test/*.test.mjs'",
    "build": "esbuild web/src/app.js --bundle --format=esm --target=es2022 --minify --outfile=web/app.js --define:process.env.NODE_ENV=\"'production'\"",
    "dev": "python3 -m http.server 8123 --directory web",
    "build:test": "esbuild web/src/app.js --bundle --format=iife --target=es2022 --outfile=.tmp/app.iife.js --define:process.env.NODE_ENV=\"'production'\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@scure/base": "^2.4.0",
    "@stacks/bitcoin-staking": "^7.6.0",
    "@stacks/common": "^7.6.0",
    "@stacks/network": "^7.6.0",
    "@stacks/transactions": "^7.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1"
  }
}
